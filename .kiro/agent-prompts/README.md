# Kiro Agent Prompts

These prompt files are used by the JSON Kiro CLI agent configs in `.kiro/agents`.

Kiro IDE custom subagents are the top-level Markdown files in `.kiro/agents`. Keep each prompt here aligned with its matching IDE subagent file.
